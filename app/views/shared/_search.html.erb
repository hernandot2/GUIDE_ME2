 <div class="search-container">
  <%= form_with(url: search_programs_path, method: :get, local: true) do |f| %>
    <%= f.text_field :query, placeholder: "Buscar por programa..." %>
    <%= f.submit "Buscar", class: "btn-primary" %>
  <% end %>
</div>
